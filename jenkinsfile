pipeline {
    agent any

    stages {

        stage('Checkout') {
            steps {
                git 'https://github.com/shameerkhan734-spec/'
            }
        }

        stage('Deploy') {
            steps {
                sh '''
                sudo cp -r * /var/www/html/
                '''
            }
        }
    }
}
